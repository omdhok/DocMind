# Copy this file to .env and fill in values as needed.
# .env is gitignored -- never commit real API keys.

# Only required if you want to use "Cloud Comparison Mode" in the UI.
# Private Offline Mode (the default and core mode) does NOT need this.
GROQ_API_KEY=
